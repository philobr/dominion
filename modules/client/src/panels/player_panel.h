#pragma once

#include <wx/wx.h>

namespace client
{
    class PlayerPanel : public wxPanel
    {
    public:
        PlayerPanel(wxWindow *parent, wxSize size);

    private:
    };

} // namespace client
